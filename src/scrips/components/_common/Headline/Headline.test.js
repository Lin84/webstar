function createTestProps(props) {
    return {
        // common props
        text: 'foo',
        ...props,
    };
}

describe('rendering Headline', () => {
    it('renders Headline react component');
});
